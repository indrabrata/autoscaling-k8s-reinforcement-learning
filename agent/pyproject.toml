[project]
name = "agent"
version = "0.1.0"
description = "Agent Autoscaler for Kubernetes using Reinforcement Learning"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=6.29.5",
    "kubernetes>=33.1.0",
    "numpy>=2.1.3",
    "prometheus-api-client>=0.6.0",
    "python-dotenv>=1.1.1",
]
