apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: resource-intensive-q-metrics
  namespace: default
  labels:
    app: kube-prometheus-stack
    release: prometheus
    prometheus: kube-prometheus-stack
    algorithm: q-learning
spec:
  selector:
    matchLabels:
      app: resource-intensive-q
  endpoints:
    - port: http
      path: /metrics
      interval: 5s
      scrapeTimeout: 3s
